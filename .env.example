# Supabase (опционально).
# Если не задано — сайт использует статический каталог из src/data.ts.
# Заполните значениями из Supabase Dashboard → Project Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
